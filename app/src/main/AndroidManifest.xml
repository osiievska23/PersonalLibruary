<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.valentina.valentina_libruary">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Manage.Add" />
        <activity
            android:name=".Drawer.Drawer"
            android:label="@string/title_activity_drawer"
            android:theme="@style/AppTheme" />
        <activity android:name=".Manage.Home" />
        <activity android:name=".TablayoutDemo.TablayoutDemo" />
        <activity
            android:name=".Manage.Tabbar"
            android:label="@string/title_activity_tabbar"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>